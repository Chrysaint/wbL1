// Выводит только последний элемент ("test"), остальное undefined так как принимае он строку,
// но при вызове функции он не возвращает эту строку в js, а только лишь в document.

document.write(document.write(document.write("test")));
